using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tutorial : MonoBehaviour
{
    [SerializeField]
    GameObject dedito;

 
    // Start is called before the first frame update
    void Start()
    {
        dedito = GetComponent<GameObject>();
       
    }

    // Update is called once per frame
    void Update()
    {
         
    }
}
